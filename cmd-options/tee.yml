command:
  providerFor:
    - tee
  categories:
    - PRINT
  prefix: SC-TEE
  scenarios:
    - cmd: $0 [PATH...]
      options:
        booleans:
          - -a
          - -i
      name: SC-TEE