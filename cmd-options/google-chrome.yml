command:
  providerFor:
    - google-chrome
    - chromedriver
  prefix: SC-GOOGLE-CHROME
  scenarios:
    - cmd: $0 <URL>
      options:
        srtings:
          - --app
          - --proxy-server
          - --proxy-pac-url
          - --password-store
        paths:
          - --user-data-dir
        booleans:
          - --incognito
      name: SC-GOOGLE-CHROME
