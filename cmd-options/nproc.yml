command:
  providerFor:
    - nproc
  categories:
    - SYSTEM_INFO
  prefix: SC-NPROC
  scenarios:
    - cmd: $0
      options:
        booleans:
          - --all
          - --help
          - --version
        strings:
          - --ignore
      name: SC-NPROC