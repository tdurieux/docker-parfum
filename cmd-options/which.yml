command:
  providerFor:
    - which
  categories:
    - UTILS
  prefix: SC-WHICH
  options: &options
    booleans:
      - -a
      - -s
  scenarios:
    - cmd: $0 [program...]
      options: *options
      name: SC-WHICH