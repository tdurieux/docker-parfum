command:
  providerFor:
    - docker-php-ext-install
  categories:
    - PACKAGE_MANAGEMENT
  prefix: SC-DPHP-EXT-INSTALL
  scenarios:
    - cmd: $0 [packages...]
      options:
        strings:
          - -j
      name: SC-DPHP-EXT-INSTALL
      fixupNonSpacedArgs: true
      categories:
        - INSTALL_PACKAGES