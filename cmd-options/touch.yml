command:
  providerFor:
    - touch
  categories:
    - FILE_SYSTEM
  prefix: SC-TOUCH
  scenarios:
    - cmd: $0 [PATH...]
      options:
        strings:
          - -A
          - -d
          - -t
          - -r
        booleans:
          - -a
          - -c
          - -h
          - -m
      name: SC-TOUCH