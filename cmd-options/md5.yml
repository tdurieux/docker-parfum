command:
  providerFor:
    - md5
  categories:
    - HASH
  prefix: SC-MD5
  scenarios:
    - cmd: $0 [paths...]
      options:
        booleans:
          - -p
          - -q
          - -r
          - -t
          - -x
        strings:
          - -s
      name: SC-MD5