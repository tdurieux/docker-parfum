command:
  providerFor:
    - cat
  prefix: SC-CAT
  scenarios:
    - cmd: $0 [PATH...]
      options:
        strings:
          - -b
          - -n
          - -l
        booleans:
          - -e
          - -s
          - -t
          - -u
          - -v
      name: SC-CAT