command:
  providerFor:
    - wc
  categories:
    - UTILS
  prefix: SC-WC
  scenarios:
    - cmd: $0 [paths...]
      options:
        booleans:
          - -c
          - -l
          - -m
          - -w
      name: SC-WC